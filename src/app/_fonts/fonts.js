import { Lexend_Peta, Montserrat } from "next/font/google";
import "./globals.css";

export const lexend_peta = Lexend_Peta({
  weight: "300",
  subsets: ["latin"],
  display: "swap",
});

export const montserrat = Montserrat({
  weight: "300",
  subsets: ["latin"],
  display: "swap",
});
